# Real SSH saved-recipe journey: ready, not executed

1. State: new `test-terminal-ssh-macos.py`, Python syntax checked only. No browser or SSH connection launched by this worker.
2. Change: bounded real Chubs SSH test using synthetic saved setups/profile/home. It checks direct SSH followed by ordered remote printf rows, explicit local one-shot SSH followed by a local marker, and actual remote exit23 stopping later local rows. The recipe's real failure notice must appear before checking the forbidden local file is absent.
3. Overall: prove saved terminal commands run in the intended location and order, rather than assuming a prompt means connection readiness.
4. Not covered: Settings editor click journey (other tests cover it), password/interactive authentication, SSH session resilience, paid/authenticated AI tools. Remote commands only print unique markers or deliberately exit23; no remote file writes, agent forwarding, or remote process left running.
5. Safety/proof: only `chubs@100.92.30.93` accepted. Exact preexisting host entry extracted with `ssh-keygen -F` into the synthetic home; unknown host refuses before launching UI. Uses StrictHostKeyChecking=yes, BatchMode=yes, short connection/keepalive limits, no config file, global known-host fallback, multiplexed connection, update-host-key write, X11/agent/TCP forwarding. Existing SSH agent required unless an explicit existing identity path is supplied. The test never reads/copies private-key bytes. Browser PID/profile verified; only owned process/sessions cleaned. Actual execution remains parent-owned.
6. Parent command: `python3 scripts/terminal-tabs/test-terminal-ssh-macos.py --app '.terminal-test/Zen Terminal Test 156.app' --label ssh156 --ssh-host chubs@100.92.30.93` (optionally `--identity-file /absolute/existing/key` if agent unavailable). High reasoning for auth/ordering failures; proof actual SSH exit/output plus local file markers.
7. Drift clarification: a direct `ssh host` recipe row deliberately makes later rows remote. It must NOT be described as followed by a local row. The separate one-shot case explicitly uses `env /usr/bin/ssh ... remote-command` as a local command, which returns before the following local row. This makes both intended locations testable without altering production semantics.
